# 식단 매니저 — 무료 배포 가이드

이 패키지(zip) 안의 파일들을 무료 호스팅에 올리면, **홈 화면에 아이콘으로 설치되고 오프라인에서도 열리는 진짜 앱(PWA)**이 됩니다. 서버·개발자 계정·비용이 전혀 필요 없어요.

> 솔직한 안내: 구글 플레이는 등록비 $25, 애플 앱스토어는 연 $99가 들어서 "무료 배포"가 아닙니다. 이 가이드의 웹 배포 방식은 완전 무료이고, 설치 경험(홈 화면 아이콘·전체화면·오프라인)은 스토어 앱과 거의 같습니다.

## 패키지 구성

```
index.html            ← 앱 본체 (식단매니저.html과 동일)
manifest.webmanifest  ← 앱 이름·아이콘 정의
sw.js                 ← 오프라인 캐시 + 업데이트 담당
icons/                ← 앱 아이콘 4종
배포가이드.md          ← 이 문서
```

## 방법 A — GitHub Pages (추천 · 완전 무료 · 영구)

1. https://github.com 에서 무료 계정을 만듭니다(이미 있으면 로그인).
2. 오른쪽 위 **+ → New repository** → 이름은 아무거나(예: `meal-app`) → **Public** 선택 → Create.
3. 만들어진 저장소에서 **uploading an existing file** 링크 클릭 → 이 zip을 **풀어서 나온 파일·폴더 전부**를 드래그해 올리고 **Commit changes**.
   (반드시 zip을 푼 내용물을 올리세요. zip 파일 자체를 올리면 안 돼요. `icons` 폴더도 함께.)
4. 저장소의 **Settings → Pages** → Branch를 `main`, 폴더 `/ (root)` 선택 → Save.
5. 1~2분 뒤 `https://아이디.github.io/meal-app/` 주소가 생깁니다. 끝!

## 방법 B — Netlify (드래그 앤 드롭)

1. https://app.netlify.com 무료 가입 → **Add new site → Deploy manually**.
2. zip을 푼 **폴더째** 드래그하면 몇 초 만에 `https://아무이름.netlify.app` 주소가 생깁니다.

## 폰에 설치하기 (주소가 생긴 뒤)

- **안드로이드 (크롬·엣지)**: 주소로 접속 → [데이터] 탭의 **📲 홈 화면에 설치** 버튼, 또는 브라우저 메뉴(⋮) → **앱 설치**.
- **아이폰 (사파리)**: 주소로 접속 → 하단 **공유 버튼(□↑)** → **홈 화면에 추가**.
- 설치하면 홈 화면에 밥그릇 아이콘이 생기고, 주소창 없는 전체화면 앱으로 열립니다. **한 번 연 뒤에는 인터넷이 끊겨도 열려요.**

## 다른 사람에게 나눠주기

주소만 보내면 끝입니다. 받은 사람도 같은 방법으로 홈 화면에 설치하면 돼요. 이 앱은 **로그인이 없고 서버도 없어서**, 각자의 기록은 각자 기기에만 저장됩니다. 받는 사람이 따로 가입하거나 설정할 것은 전혀 없어요.

## 나중에 앱을 수정했을 때 (업데이트 배포)

1. 새 `식단매니저.html`을 `index.html`로 이름을 바꿔 저장소에 다시 올립니다(덮어쓰기).
2. `sw.js` 파일을 열어 맨 위 `const VER = "22.0.0"` 숫자를 올립니다(예: `22.0.1`) → 같이 올립니다.
3. 사용자가 앱을 열면 "새 버전이 준비됐어요 → 업데이트" 바가 자동으로 뜹니다.

## 자주 묻는 것

- **비용이 정말 0원인가요?** 네. GitHub Pages·Netlify 무료 플랜으로 충분합니다(개인용 트래픽 기준).
- **파일로 쓰던 기록은요?** 웹주소 버전은 브라우저 입장에서 '다른 곳'이라 기록이 자동으로 옮겨지진 않아요. 기존 파일의 [데이터] → 내보내기 → 웹주소 버전에서 가져오기 한 번이면 이어집니다.
- **카카오톡으로 파일 보내던 방식은요?** 그대로 됩니다. 이 앱은 파일로 열어도 모든 기능이 동일하게 동작해요.
- **가입이나 로그인은요?** 없습니다. 열면 사용법 안내 몇 장을 넘긴 뒤 바로 시작해요. 인터넷이 없어도 전부 동작합니다.
