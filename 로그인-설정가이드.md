# 로그인·그룹 기능 켜기 — Firebase 설정 (무료 · 약 10분)

앱의 로그인·계정 동기화·그룹(초대 코드) 기능은 **형록님 소유의 무료 Firebase 프로젝트**에 연결해야 실제로 동작합니다. 설정 전에는 앱이 **게스트 모드**(이 기기 저장)로 모든 기능이 정상 동작하니, 배포 준비가 되면 아래를 딱 한 번만 하면 됩니다.

## ⭐ 먼저: 이 설정은 누가 하나요? — 사용자는 "설정 없는 시작(Zero Setup)"

혼동하기 쉬운 부분이라 먼저 정리합니다. Firebase 설정은 **앱을 배포하는 형록님이 단 한 번** 하는 개발자용 작업이고, **앱을 내려받는 일반 사용자는 이 과정을 절대 거치지 않습니다.** 카카오·토스 같은 앱에서 사용자가 서버나 DB를 만들지 않는 것과 똑같습니다. 한 번 배포된 뒤 사용자의 경험은 이렇게 됩니다:

- **가입/로그인만 하면 끝** — 이메일·비밀번호로 가입하는 순간, 그 사용자만의 데이터 공간(`users/{uid}`)이 백그라운드에서 자동으로 만들어집니다. 데이터베이스 생성·프로젝트 연결 같은 기술적 설정은 **사용자에게 전혀 요구되지 않습니다.**
- **그룹 참여도 원탭** — 그룹장은 [＋ 그룹 만들기] 버튼 한 번이면 6자리 코드·초대 링크·QR이 즉시 생성됩니다. 상대는 **① 코드 입력, ② 초대 링크 클릭, ③ QR 스캔** 중 아무거나 하면 그 자리에서 참여됩니다. 초대 링크로 들어온 사람은 가입/로그인만 하면 **자동으로 그 그룹에 합류**합니다(따로 코드 입력조차 필요 없음).
- **공유 수단** — 그룹 카드의 [📤 공유하기]는 카카오톡·문자·기타 앱으로 바로 보내는 시스템 공유창을 열고, [📷 QR]·[코드 복사]·[링크 복사] 버튼도 함께 제공합니다.

즉, 아래 10분짜리 설정은 **형록님(배포자)이 딱 한 번** 하는 것이고, 이후 모든 사용자는 설정이 0입니다.

## 1. Firebase 프로젝트 만들기
1. https://console.firebase.google.com → **프로젝트 추가** → 이름 입력(예: meal-app) → 애널리틱스는 꺼도 됨 → 생성.

## 2. 로그인 방식 켜기 (이메일)
1. 왼쪽 **빌드 → Authentication → 시작하기**.
2. **Sign-in method** 탭 → **이메일/비밀번호** → 사용 설정 → 저장.
   - (선택) 나중에 Google 로그인을 추가하려면 여기서 Google도 켜면 됩니다. Apple 로그인은 애플 개발자 계정 연동이 추가로 필요합니다.

## 3. 데이터베이스 만들기
1. 왼쪽 **빌드 → Realtime Database → 데이터베이스 만들기**.
2. 위치는 **아시아(예: 싱가포르 asia-southeast1)** 선택 → **잠금 모드**로 시작 → 사용 설정.
3. **규칙(Rules)** 탭에 아래를 붙여넣고 **게시**:
```json
{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "groups": {
      "$code": {
        ".read": "auth != null && (data.child('members').child(auth.uid).exists() || !data.exists())",
        ".write": "auth != null && (!data.exists() || data.child('members').child(auth.uid).exists())"
      }
    },
    "rooms": { ".read": true, ".write": true }
  }
}
```
> 이 규칙은 ①내 계정 데이터는 나만 ②그룹 데이터는 그 그룹 멤버만 읽고 쓰도록 막아 줍니다. (rooms는 구버전 '함께 쓰기' 호환용)

## 4. 앱에 설정값 넣기 — 두 가지 방법

먼저 설정값을 복사합니다:
1. Firebase 콘솔 왼쪽 위 **⚙︎ → 프로젝트 설정** → 아래로 스크롤 → **내 앱**에서 **웹(&lt;/&gt;)** 추가 → 닉네임 입력 → 등록.
2. 나오는 `firebaseConfig` 부분을 **통째로 복사**합니다(주변에 import 같은 줄이 섞여 있어도 괜찮아요 — 앱이 알아서 골라냅니다).

### 방법 A — 앱에서 붙여넣기 (추천 · 파일 수정 없음 · 1분)
1. 앱을 열고 **왼쪽 위 ⚙︎(설정)** 을 누릅니다. (하단 탭이 아니라 화면 맨 위 왼쪽의 톱니 버튼이에요!)
2. **[👤 계정]** 카드의 **"🔧 배포자용 — 설정 붙여넣기로 바로 켜기"** 를 눌러 펼칩니다.
3. 복사한 firebaseConfig를 붙여넣고 **[저장하고 켜기]** → 그 즉시 **로그인 화면**이 나타납니다. 끝!
> 설정이 켜진 앱은 실제 앱처럼 **시작할 때 로그인 화면부터** 나옵니다: 로그인/회원가입 → 통과 → 앱 이용. 한 번 로그인하면 다음부터는 자동으로 통과되고, 로그아웃하면 다시 로그인 화면으로 돌아갑니다. (하단의 '로그인 없이 둘러보기'는 게스트용 — 앱스토어 심사에도 필요한 요소라 남겨 뒀어요)
   - 이 방법은 **지금 쓰는 기기(브라우저)에 저장**됩니다. 본인 확인·테스트용으로 가장 빠르고, 실수할 곳이 없어요.
   - 다른 사람에게 나눠줄 배포 파일에는 아래 방법 B로 심어 두는 것을 권장합니다(모든 기기에서 자동 적용).

### 방법 B — 파일에 직접 넣기 (배포용)
1. `식단매니저.html`(또는 배포 폴더의 `index.html`)을 **메모장·코드 편집기**로 엽니다. (워드/한글 프로그램은 파일을 망가뜨리니 금지!)
2. `let APP_FIREBASE = null;` 줄을 찾아(Ctrl+F로 `APP_FIREBASE` 검색) 아래처럼 바꿉니다:
```js
let APP_FIREBASE = { apiKey:"AIza...", authDomain:"meal-app.firebaseapp.com",
  databaseURL:"https://meal-app-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId:"meal-app", appId:"1:123...:web:abc..." };
```
3. 저장 → **바로 그 파일**을 열면 ⚙︎ 설정 안에 로그인 카드가 나타납니다.
   ⚠️ `databaseURL`이 있어야 동기화·그룹이 켜집니다(없으면 로그인만 동작 — 아래 문제 해결 6번).

## 5. 문제 해결 — "가이드대로 했는데 로그인이 안 돼요"

**⭐ 가장 빠른 방법: 앱에 진단 도구가 있어요.** 시작 화면(또는 ⚙︎ 설정의 계정 카드) 아래 **[로그인이 안 되나요? 문제 진단]** 을 누르면, 앱이 실제 인증 서버에 테스트 가입을 보내 **어느 단계가 막혔는지 ✅/❌로 알려주고 해결법까지 표시**합니다(테스트 계정은 즉시 자동 삭제). ❌ 항목의 안내대로 고친 뒤 [다시 진단]을 누르세요. 해결이 어려우면 [결과 복사]를 눌러 그대로 보내 주세요.

그 외 순서대로 확인하세요. 대부분 1~3번에서 해결됩니다.

1. **로그인 카드 위치**: 하단 탭(오늘/식단표/레시피/피드/재고/주문)에는 없습니다. **왼쪽 위 ⚙︎(톱니)** 를 눌러야 [👤 계정] 카드가 보여요.
2. **옛 버전 파일인지 확인**: ⚙︎ 설정 화면 **맨 아래에 "식단 매니저 v21.1"** 이라고 나와야 합니다. 버전이 다르거나 [👤 계정] 카드 자체가 없다면, 예전에 받은 다른 사본을 열고 있는 것입니다(카톡·다운로드 폴더에 사본이 여러 개 쌓이기 쉬워요). 최신 파일 하나만 남기세요.
3. **"설정 붙여넣기" 문구가 계속 보인다면**: 수정한 파일과 지금 연 파일이 **서로 다른 파일**이라는 뜻입니다. 이 경우 파일을 찾지 말고 그냥 **방법 A(앱에서 붙여넣기)** 를 쓰면 파일과 무관하게 즉시 켜집니다.
4. **웹(GitHub Pages 등)에 올린 경우**: 새 index.html 업로드 후 **두 번 새로고침** 해야 캐시가 갱신됩니다. (홈 화면에 설치한 앱도 동일)
5. **"설정을 인식하지 못했어요"**: firebaseConfig의 **중괄호 { } 를 포함해 통째로** 복사했는지 확인하세요. 일부만 복사하면 인식이 안 됩니다.
6. **"동기화·그룹이 꺼져 있어요" 경고**: 설정에 `databaseURL`이 없는 경우입니다. 최신 Firebase 콘솔은 **Realtime Database를 만들기 전엔** 이 값을 안 보여줍니다 → 가이드 **3번(데이터베이스 만들기)** 을 먼저 마친 뒤, 프로젝트 설정에서 firebaseConfig를 **다시 복사**해 넣으세요.
7. **가입을 눌렀는데 오류가 나요**: 오류 문구가 알려줍니다 —
   - "이메일/비밀번호 로그인이 꺼져 있어요" → 가이드 2번(Authentication → Sign-in method)에서 사용 설정.
   - "apiKey가 올바르지 않아요" → 콘솔에서 firebaseConfig를 다시 복사.
   - "로그인 준비가 아직 안 됐어요 / 로그인 모듈을 불러오지 못했어요" → 인터넷 연결 확인 후 [다시 시도]. 카카오톡 인앱 화면이라면 **사파리·크롬으로 열기**.
8. 그래도 안 되면: firebaseConfig 값을 저(Claude)에게 붙여넣어 주세요 — 설정을 심은 완성본 파일을 만들어 드립니다. (웹용 apiKey는 비밀번호가 아니라 공개 식별자라 공유해도 안전합니다)

## 6. 소셜·전화 로그인 켜기 (구글 · Apple · 전화번호)

앱 시작 화면에 **Google로 계속하기 / Apple로 계속하기 / 전화번호로 계속하기** 버튼이 준비돼 있습니다. 각 버튼은 Firebase 콘솔에서 해당 방식을 **켜야** 동작하고, 켜기 전에 누르면 앱이 한국어로 무엇을 켜야 하는지 알려줍니다.

> 소셜·전화 로그인은 **배포된 웹 주소(https)** 에서 동작합니다(팝업·보안문자 방식이 웹 표준이라서요). 파일로 직접 열거나 앱 내 화면에서는 이메일 로그인을 쓰세요. 또한 콘솔 **Authentication → Settings → 승인된 도메인**에 배포 주소(예: `아이디.github.io`)를 추가해야 합니다.

**⭐ 구글 로그인 (가장 쉬움 · 무료 · 2분)**
1. 콘솔 → Authentication → Sign-in method → **Google** → 사용 설정 → 지원 이메일 선택 → 저장.
2. 승인된 도메인에 배포 주소 추가. 끝 — 앱의 [Google로 계속하기]가 바로 동작합니다.

**Apple 로그인 (Apple 개발자 계정 필요 · ~30분)**
1. Apple Developer에서: Identifiers → App ID에 **Sign In with Apple** 활성화 → **Services ID** 생성 → 웹 도메인과 Return URL(`https://프로젝트ID.firebaseapp.com/__/auth/handler`) 등록 → Key 생성(.p8 다운로드).
2. 콘솔 → Sign-in method → **Apple** 사용 설정 → Services ID·팀 ID·키 ID·키 파일 입력.
3. 앱스토어 출시 시엔 어차피 개발자 계정($99/년)을 만드니 그때 같이 하면 됩니다. ※ 애플 정책: iOS 앱에 소셜 로그인을 넣으면 Apple 로그인이 필수 — 이 앱은 이미 버튼이 준비돼 있어 조건 충족.

**전화번호 로그인 (⚠️ 유료 요금제 연결 필요)**
- 2024년 9월부터 전화 인증(SMS)은 **Blaze 요금제(카드 등록) 연결이 필수**입니다. 무료(Spark) 플랜에선 문자가 아예 발송되지 않아요. Blaze 연결 후엔 **하루 10건까지 무료**, 초과분은 건당 과금(한국 기준 소액).
- 켜는 법: 콘솔 → Sign-in method → **전화** 사용 설정 + 프로젝트를 Blaze로 업그레이드.
- 과금 없이 시험하려면: Sign-in method → 전화 → **테스트용 전화번호**에 번호와 고정 인증번호를 등록하면 실제 문자 없이 로그인 흐름을 확인할 수 있습니다.

**카카오·네이버 로그인 (현재 미지원 — 정직한 안내)**
- 카카오·네이버는 Firebase가 직접 지원하지 않아, **별도 인증 서버**(커스텀 토큰 발급)가 있어야 합니다. 서버 없이 넣으면 보안 규칙이 뚫리는 가짜 로그인이 돼서 넣지 않았어요. 원하시면 Cloud Functions(Blaze) 기반으로 다음 단계에서 추가할 수 있습니다.
- 참고: 이메일·구글·Apple·전화 로그인은 서버 없이 완전하게 동작합니다.

## 무료 사용량
- Firebase Authentication과 Realtime Database 무료 플랜(Spark)은 개인·소규모 사용에 충분합니다(동시접속 100명, 저장 1GB, 다운로드 월 10GB). 초과 시에만 종량제로 전환됩니다.

## 소셜 로그인(카카오·네이버·구글·애플)에 대해
- **구글·애플**은 Firebase가 직접 지원합니다(콘솔에서 켜고 약간의 설정 추가). 특히 앱스토어에 낼 때 **소셜 로그인을 하나라도 넣으면 애플 정책상 'Apple로 로그인'도 함께 넣어야** 합니다.
- **카카오·네이버**는 Firebase가 기본 지원하지 않아, 별도의 인증 서버(Cloud Functions로 커스텀 토큰 발급)가 필요합니다 — 무료 호스팅만으로는 어렵고 서버 작업이 추가됩니다.
- 그래서 이번 버전은 **가장 안정적이고 서버가 필요 없는 이메일/비밀번호**를 기본으로 완성했습니다. 구글/애플 로그인 추가를 원하시면 말씀해 주세요 — 그 두 개는 붙일 수 있습니다.
